import { CompareItemPipe } from './compare-item.pipe';

describe('CompareItemPipe', () => {
  it('create an instance', () => {
    const pipe = new CompareItemPipe();
    expect(pipe).toBeTruthy();
  });
});
