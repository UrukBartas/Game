import { ItemdataToItemPipe } from './itemdata-to-item.pipe';

describe('ItemdataToItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemdataToItemPipe();
    expect(pipe).toBeTruthy();
  });
});
